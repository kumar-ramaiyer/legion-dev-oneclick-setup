# Legion Development Environment Setup Requirements
# Python packages required for the setup script

# Core dependencies (essential for setup to work)
PyYAML==6.0.2
mysql-connector-python>=8.0.0
requests>=2.25.0
gdown>=4.7.0
python-dotenv>=0.19.0
psutil>=5.8.0

# Note: docker-compose and localstack removed due to Python 3.13 compatibility issues
# They are not needed for the setup script itself - Docker operations use the docker CLI directly